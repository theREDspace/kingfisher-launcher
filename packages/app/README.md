# @redspace/kingfisher-launcher-app

The React (Vite) frontend for Kingfisher Launcher. Its build output is embedded into the `@redspace/kingfisher-launcher` CLI package.

See the [main README](../../README.md) for what this project does, prerequisites, and development instructions.
